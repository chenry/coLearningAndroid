<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.colearning.android.podcastcatcher"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SubscriptionListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SubscriptionDetailActivity"
            android:label="@string/title_activity_subscription_detail" >
        </activity>

        <service android:name="com.colearning.android.podcastcatcher.service.UpdatePodcastSubscriptionService" />

        <provider
            android:name="com.colearning.android.podcastcatcher.contentProvider.PodcastCatcherContentProvider"
            android:authorities="com.colearning.android.podcastcatcher"
            android:multiprocess="true" >
        </provider>
    </application>

</manifest>